import { Canvas, Meta } from '@storybook/blocks';
import * as Stories from './Text.stories';

<Meta of={Stories} />

# Text

The Text component renders a container around text.

## Default Text

It renders a `p` tag by default.

<Canvas of={Stories.Default} />

## Styled Text

Use the optional `sx` prop for styling.

<Canvas of={Stories.StyledText} />

## HTML Tags

Use the optional `as` prop to declare the HTML tag used to render the Text.

<Canvas of={Stories.OrderedList} />

### Headings

<Canvas of={Stories.Heading1} />
<Canvas of={Stories.Heading2} />
<Canvas of={Stories.Heading3} />
