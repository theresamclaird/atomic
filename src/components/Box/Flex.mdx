import { Canvas, Meta, Controls } from '@storybook/blocks';
import * as Stories from './Flex.stories';

<Meta of={Stories} />

# Flex

Flex is just a Box with `display: flex` applied and some convenience props specific to Flexbox CSS.

## Example

<Canvas of={Stories.FlexBox} />
<Controls of={Stories.FlexBox} />