import { Canvas, Meta, Controls } from '@storybook/blocks';
import * as Stories from './Image.stories';

<Meta of={Stories} />

# Image

Renders a responsive image with `picture`, `source`, and `img` tags.

<Canvas of={Stories.Default} />
<Controls of={Stories.Default} />

## Responsive Image

<Canvas of={Stories.ResponsiveImage} />
<Controls of={Stories.ResponsiveImage} />

## Aspect Ratio Image

Supplying an optional `aspectRatio` prop will wrap the image in a containing Box. This also preserves space for the image as it loads, preventing content shift.

<Canvas of={Stories.AspectRatioImage} />
<Controls of={Stories.AspectRatioImage} />