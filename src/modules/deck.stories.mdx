import { Meta, Story, Canvas } from '@storybook/addon-docs';

<Meta title="Modules/getDeck()" />

# getDeck()

The `getDeck()` function returns an entire deck of standard playing cards as an array of card objects.

## Use

```jsx
import { getDeck } from '@theresamclaird/atomic';

const deck = getDeck();

```

